/** 
 * Multiplexor:
 * if (sel = 0) out = a, else out = b
 */
CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    Not(in =sel, out =temp1);
    And(a =a, b =temp1, out =temp2);
    And(a =b, b= sel, out =temp3);
    Or(a =temp2, b =temp3, out = out);
}
